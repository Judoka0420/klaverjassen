{
  "name": "klaverjassen-app",
  "version": "1.0.0",
  "private": true,
  "description": "Klaverjassen (Rotterdam rules) — offline solo + peer-to-peer multiplayer mobile app",
  "scripts": {
    "sync-assets": "node scripts/sync-assets.js",
    "serve": "node scripts/sync-assets.js && node scripts/serve.js",
    "prepare-www": "npm run sync-assets",
    "cap:sync": "npm run sync-assets && cap sync",
    "cap:add:android": "npm run sync-assets && cap add android",
    "build:android": "npm run sync-assets && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "peerjs": "^1.5.4"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
